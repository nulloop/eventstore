syntax = "proto3";

package event;

message Transport {
  string correlationId = 1;
  string signature = 2;
  string topic = 3;
  bytes payload = 4;
}

// the following DummyMessage and Dummy enum are only used in 
// tests

message DummyMessage {
  string value = 1;
}
