syntax = "proto3";

package event;

message Transport {
  string id = 1;
  bytes payload = 4;
}

// the following DummyMessage and Dummy enum are only used in 
// tests

message DummyMessage {
  string value = 1;
}
